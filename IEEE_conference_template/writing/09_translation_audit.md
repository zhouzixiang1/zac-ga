# 表 IV 与英文全文交付核查

日期：2026-09-06。中文稿为内容依据；本轮未更改实验数据或重新运行实验。

## 本轮修改

- 表 IV 保持单栏三线表，参数名不再强制拆行，取值居中，结果列右对齐，四组参数采用一致留白。
- 新增完整英文稿 `paper_en.tex` 和 `sections_en/`，与中文同名文件逐节对应；包括标题、作者信息、摘要、正文、算法、图注和表注。
- 英文统一使用层边界、驻留、门位、存储位和物理前瞻术语。调整英文句界和冗余措辞，保留中文的全部段落论点、约束条件、比较范围和结果。
- 图 6 的前置宏定义原先在 `resizebox` 中产生水平空白；现抑制这些行尾空格，使共享矢量图正常居中。图中数据、坐标轴范围和标注均未改动。
- 英文算法拆分过长步骤以保留清楚的缩进层级；搜索条件与控制流不变。
- 英文末页使用不拉伸、不压缩正文的 `flushend` 列平衡。正文和页边距仍采用 IEEEtran 默认设置。

## 对照结果

| 检查项 | 结果 |
|---|---|
| 中文分页 | 9 页；参考文献在第 9 页 |
| 英文分页 | 11 页；参考文献在第 11 页 |
| 公式 | 20 个编号公式及标签对应 |
| 图与表 | 两版各 6 张图、4 张表，复用同一份图源和数值宏 |
| 引用 | 各节引用键对应，共用 `references.bib` |
| 数值 | 结果宏调用核对通过；无手工重录结果 |
| 编译问题 | 无 overfull、未定义引用、未定义文献或过大浮动体 |
| 英文 PDF | 无中文字符；全部字体嵌入，无 raster 图像对象 |
| 回归测试 | 49 项写作/验收测试、13 项图数据测试、16 项同步准备测试通过 |

显式翻译的数学环境内文字仅包括“存在”→“exists”和算法种子说明；数学运算与参数不变。
有限视界末端的回迁代价未改写成电路结束后的实际回迁，未来评价仍从候选产生的状态开始。
逐电路选例条件、共同有效电路范围、几何均值定义和端到端编译时间比较均保留。

英文 11 页已逐页查看高分辨率渲染。中文检查了九页总览，并重点复核表 IV 所在第 7 页与图 6 所在第 8 页。
表 IV、算法、图注和最终正文页未见可见重叠或出界。自动对应检查不能替代作者对技术含义的最终确认。

## 人工审阅入口

- 中文：`../paper_zh.tex` 与 `../sections/`。
- 英文：`../paper_en.tex` 与 `../sections_en/`；相同文件名逐节对照。
- 术语：`08_translation_ledger.md`。
- PDF：仓库根 `build/paper_zh/paper_zh.pdf`、`build/paper_en/paper_en.pdf`。
- 表 IV：中文第 7 页、英文第 9 页。
- QA 和逐页预览：对应 `build/paper_zh/`、`build/paper_en/`。

两版的源码与已验收 PDF 均有构建绑定。修改科学内容后运行 `make paper-en` 重新验收；需要预览时运行 `make paper-en-preview`。
本记录为翻译完成时的验收快照；GitHub/Overleaf 同步由后续独立 Git 操作管理。Overleaf 的主文档选择不随文件同步自动切换，桌面保留副本未改动。
