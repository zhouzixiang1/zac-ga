# 方法论证、贡献与实验对应修订

本轮仅修订中文论文及派生显示值，不运行新实验，不改变冻结主结果、基线、原生内核或 `figures/data/`。英文版仍暂停同步，未提交、推送或同步 Overleaf。

## 论证与证据

| 论文主张 | 方法中的解释 | 证据及其范围 |
|---|---|---|
| 门位和驻留应共同选择 | 图4中的门位 T2 被 q3 占用；将目标门安排到 T2 必须释放该阱，回迁存储位由匹配补全 | 图4是可行输运示例。保留 q3 并另选门位需要阵列存在其他可用门位，图中不虚构第二个完整方案 |
| 遗传搜索用于求解联合目标 | 编码、交叉和变异同时调整门位及驻留，存储位分配随候选接受执行评价 | 第四节 C、图7(b)：同一联合变量、目标和预算下的 GA/greedy 比较，38 个有效电路；不是联合与顺序决策的独立消融 |
| 驻留取舍受到未来层距影响 | 图5(a)(b)：驻留承受 r 次空闲受激，回迁并再次入区包含四次阱转移 | 固定模型算例，r=1/2 时两项损失排序改变。假定两方案可行，仅比较该原子的转移及受激因子，不含相干、其他原子和完整候选选择规则 |
| 候选留下的执行起点影响后续损失 | 图5(c)：从候选后位置、占位与时钟逐层展开，累加衰减后的新增损失 | 第四节 D、图7(b)：共享参数的 H=8/H=0 比较，39 个有效电路；包括未来层、末端回迁项和近期复用驻留规则 |
| 执行代价评价可用于初始化 | 固定初始布局候选，比较首层及早期未来层的执行损失 | 初始化扩展独立评价，36 个完整电路；总体结果及动态对照仍使用原始 SA 初始化 |
| 完整方法改善编译质量 | 第四节 B：先报告两个基准集的汇总，再给出逐电路结果和机制分解 | 图7(c)固定使用冻结 `qft_n18_transpiled` 与 ZAC 的配对：转移次数 648→402；损失分量说明完整方法的结果，不分别归因于 GA 或前瞻 |

## 文字与图形

- 方法沿总体框架、门调度、初始化、层内联合优化、路由、输出的时间顺序展开；在公式前解释负对数损失，并在编码与驻留公式前给出具体问题。
- 实验按总体质量、遗传搜索、多层前瞻、参数与编译开销组织。正文直接报告结果；种子、重复计时、预定清单与未完成记录集中到[实验协议](experiment_protocol.md)，必要的统计对象与比较条件仍保留。
- 图4把 T1+/T2+、q3 回迁位 s1 与原子快照对应；图5用固定模型柱图替代未给出完整评分的 A/B 示意；图7由总体收益、受控比较和有符号损失分解组成，全部为 TikZ/PGFPlots。
- ZAIR 图安排在方法输出段内；总体表与宽图提前声明，避免 IEEE 双栏浮动积压；逐电路表与参数表优先就近放置。

## 复核入口

- [当前中文 PDF](../../build/paper_zh/paper_zh.pdf)、[自动验收](../../build/paper_zh/final_paper_qa.json)、[九页总览](../../build/paper_zh/page_overview.png)。
- [派生值生成器](generate_argument_values.py)与[数值来源](../paper_argument_values.json)区分模型算例和既有运行记录；后者仍是模型估计的保真度，不是硬件测量。
- 写作、图形语义、冻结输入和 Overleaf 保护测试使用 `make paper-test PYTHON=ZAC/.venv/bin/python`；最终构建状态以当前 QA 文件为准，不能由本记录替代。

本轮 `make paper` 与 `make paper-test PYTHON=ZAC/.venv/bin/python` 均通过：155 项写作/结构测试、13 项图形数据测试和 20 项 Overleaf 保护测试，共 188 项。PDF 为八页正文加第九页参考文献，无 overfull、未定义引用或文献；图3独立 PDF 为单页矢量且字体嵌入。冻结主结果、原生内核与图形输入无差异，`git diff --check` 通过。

末页双栏失衡来自 `flushend` 将表格横线误识别为脚注：日志产生了负的分割高度。对不含脚注的正文末页与参考页关闭这一检测后，左右栏恢复近齐底，无须增删论证或拉伸文字。验收器新增负分割高度失败检查，避免仅因页数和 overfull 检查通过就误报版式正常。

采用 nature-writing 的贡献与证据对应原则、nature-figure 的数据及图义审查、nature-polishing 的渲染后排版检查；保留作者指定的中文 IEEE 体例和 TikZ/PGFPlots。
