# 2026-09-08 中文稿标红审核说明（最新）

本轮只修改中文稿。以开始本轮修改时的工作树为基准，不以较早的 Git 提交为基准；作者此前修改的正文、图形与批注均保留。

## 审核入口

- `../build/paper_zh/paper_zh.pdf`：标红后的九页稿件。
- `../revision_marks.tex`：正文与独立 Fig. 3 共用的修订显示开关。
- `../build/paper_zh/revision_audit_current.json`：最新一轮逐项源文件差异，包括替换前的文字。删除内容无法在新 PDF 中显示，故同时保存在该记录中。
- `../build/review-baseline-20260908-1303-H0aeGM/`：最新一轮修改前的源文件和 PDF，保留作者此前同步修改；上午快照 `review-baseline-20260908-7dI6Ek/` 也保留。

## 修改范围

| 位置 | 修改内容 | 本轮证据范围 |
|---|---|---|
| 第二节末尾，两项贡献 | 删去范围过宽的“现有方法未”断言；说明初始化与动态层内评价的联系 | 保留既定两项贡献，不新增图着色或约束检查创新声明 |
| 第三节总体框架 | 不再承诺 H 比较中未展示的计时分析 | 现有五档 H 比较展示保真度比与重排批次比 |
| 第三节门调度 | 单比特指令地址来自执行时的原子映射，而非全部限定为存储区 | 对照实际指令生成代码与原生轨迹 |
| 第三节初始化 | 直接说明前瞻评价、布局选择及其作为层内优化起点的作用；末尾简要指向消融实验 | 不再用“默认启用”或执行日志式说明；H_init=2、候选上限4、rho=0.7、每层评价预算32集中列于实验设置 |
| Fig. 3 与图注 | “Look-ahead option”改为初始化环节；图注避免将示例中的首层位置推广到所有单比特门 | 图体仍为 TikZ，改动的标签为红色，区域与原子颜色不变 |
| 第四节初始化比较 | 不再称“可选扩展”；明确受控初始化比较与完整配置的区别 | 初始化消融仍使用原有同参数配对证据，不将总体配置差异当作纯初始化贡献 |
| 摘要、总体结果、表 II/III 与图 7(a) | 更新为包含前瞻初始化布局的完整质量结果，所有改动标红 | ZAC共同有效集合为16个，QMAP为120个；同一集合上的两个基线均值一并重算 |

## 新实验与旧结果

补实验已于12:06完成。389个新增任务有366个成功、4个经独立核验恢复的完整结果、9个内存上限、4个超时和6个运行错误；另118个旧结果通过复用核验。所有原结果、失败记录和冻结源码均保留，未重跑或覆盖旧主实验。空电路上的6个运行错误仍按原失败记录处理。

新主比较从 `ZAC_zzx/results/default_initial_v1/paper_exports/` 的独立生成文件载入，使用 `Default` 前缀宏，不覆盖旧 `results_values_zh.tex`。主验收核对完整结果来源、四路至六路并发变更记录，以及双方调度结束记录。增加并发可能影响限时任务的完成覆盖，因此新结果不用于串行速度结论；原遗传搜索、动态前瞻消融及串行计时仍单独保留。QFT-18的新旧机制数值完全一致，图7(c)经逐字段等价检查后保持原样。

主表与图7(a)共同有效集合已经同步更新。ZAC集合从18个变为16个，不能把新旧不同集合的均值直接相减作为初始化收益；初始化的作用仍由受控消融说明。表III按原先固定的选例规则重算，第二项从Ising-42变为Wstate-27。数据包和旧结果并存，方便作者回查。

最新核验包含九页编译、末页参考文献、数值与来源检查、标红检查及逐页视觉复核；336项论文测试与17项保全审计测试通过。原封存的20676个文件全部逐项匹配，53项论文源文件与两个PDF产物哈希也全部匹配。保全报告保存在 `../build/repository-reorganization/after-default-publication-20260908.json`。英文稿未同步；本轮未提交或推送。Overleaf下次同步前须将新增外部数据输入纳入导出包，不能直接沿用旧云端包。

## 接受修订后的操作

将 `revision_marks.tex` 中的 `\PaperShowRevisionstrue` 改为 `\PaperShowRevisionsfalse`，再从仓库根目录执行 `make paper`。这一开关同时控制正文与独立框架图，保留宏中的实际文字；不要分别修改各个图的语义配色。

审核期不移除标红宏，不覆盖作者的新改动，也不自动同步英文稿或推送 Overleaf。
