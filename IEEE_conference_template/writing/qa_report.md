# 中文论文统一修订 QA

## 当前状态

- 实验与聚合结果保持冻结；本轮更新文字、图表和派生数值的显示。正文只保留 ZAC18 和 QMAP154，直接比较对象仅为 ZAC 与 ICCAD/QMAP。
- 正文分为引言、背景与相关工作、方法、实验评估、讨论和结论六节。背景末尾设有“本文贡献”，方法与实验分别承担定义和证据，避免以实现过程或验收过程组织段落。
- 全文包含六张编号图和四张编号表。六张图均由 TikZ/PGFPlots 或独立矢量 PDF 生成，图内使用英文标签；表格数值由 `results_values_zh.tex` 与受控逐电路派生文件注入。
- Fig. 3 贯通输入、分层调度、分区初始映射、层边界联合决策、物理可行重排和 ZAIR 输出。图体保留在 `figures/overall_framework.tex`，由 XeLaTeX 强制构建为单页 `figures/overall_framework.pdf` 后插入正文；图中的逻辑原子只用于保持各阶段身份和门依赖可追踪，不限定方法的问题规模。
- Fig. 2(c)表示单条直达轨迹的静态占用冲突：静止原子位于通道上的$s_1$时，$u\to u'$不可行，拆分并行批次不能修复；重新选择通道外的存储位$s_2$或加入中转后才能得到可行路径。
- Fig. 6 是由 PGFPlots 读取受控派生数据生成的双面板分组柱状图：面板 (a) 在全部共同有效独立单元上给出总体保真度几何均值的改善百分比，面板 (b)给出四个高增益实例的逐项增幅，均分别比较ZAC和ICCAD/QMAP。两轴统一为0–45%，从零起画，柱顶标注增幅；正文不呈现置信区间、Wilcoxon、Holm或胜/平/负统计。
- Table I--IV统一使用 `booktabs` 与栏宽或全文宽度；表III与表IV在第8页并排，分别呈现典型电路和单因素参数敏感性。
- 图表引用统一为先陈述事实、再在句内引用“图/表”；子图统一为 `(a)` 形式，图注末尾保留句号，表题不加句号。正文可见文本不含内部方法编号或开发阶段动作标签。
- 当前 PDF 为九页：正文占前八页，参考文献独立位于第九页。页面版心由 `IEEEtran` 的 A4 会议模式直接控制，未使用 `geometry` 改写页边距。PDF文本核验显示Table I位于第六页、Table II位于第七页；Fig. 6以及并排的Table III和Table IV位于第八页。
- 作者、单位、地址和邮箱字段已填写；引言脚注给出GitHub仓库根地址。作者已确认本研究无经费资助；投稿前仍需核对最终元数据，机器状态以 `final_paper_qa.json` 为准。

## 方法与证据边界

- 三项技术贡献统一为：（1）门位与驻留的联合遗传搜索架构，以匹配补充回迁存储位，小规模空间使用精确枚举；（2）从候选后状态展开的衰减多层评价；（3）纳入候选评价的约束冲突图着色。两基线主比较与受控组件比较承担实验验证。
- 权威论文交付根目录为 `/Users/zhouzixiang/Desktop/zac/ZAC_zzx/results/paper_zh_v2`，其 `final_manifest.json` 协议为 `paper-zh-v2-final-manifest-v1`，SHA-256为 `a58a6d953ed282d1a44bf0dfb096ef8790b5d05fbfa7397322af43023c5454e6`。
- `paper_values.json` 是结构化数值源；证据目录和论文仓库中的 `results_values_zh.tex` 均含178个宏，SHA-256均为 `70791d424ae8b4e58a25c61412d4be334984475f3ac88e685318a11877a7d62d`。
- `four_methods_results.xlsx` 只含 `ZAC18` 与 `QMAP154` 两个工作表，SHA-256为 `5c0804b57eab8712a3dc0cc29ce62506c6ac2f1baceccd23be0e11a91c107ac3`；`main_primary_analysis_units.csv` 的SHA-256为 `8f03d7ebc3e96f135ffc390f44427ec4f6c1b607d4f61461aaef27eb8082f276`。
- 正文在引言脚注中链接GitHub仓库根地址 `https://github.com/zhouzixiang1/zac-ga`，未固定到某个提交。仓库当前为私有，未获授权的审稿人无法直接访问链接。
- 方法定义已对照工程实现：软驻留保护计数只统计被候选回迁的保护原子；强制回迁先于该计数执行。候选投影、预测可行性、准入锚点和预测失败时的当前状态回退均在正文中明确定义。
- 每个未来层的物理增量分解为空闲驻留、门参与原子输运和阻挡原子迁移三项。终端项是经过轨迹约束检验的候选相关清理代价，而非统一距离代理。
- 配置的最大视界为八层；实际可见深度同时受电路末端、裸衰减因子阈值以及长电路资源上限约束。受控视界比较共享$(\alpha,\rho,\epsilon)=(0.5,0.7,0.05)$。`H_{\max}=0` 令可见未来层集合及相应驻留保护集合为空，也不计算窗口终端清理势，同时保留当前边界的物理约束与候选构造规则。
- 正文分别报告GA-LK相对ZAC和ICCAD/QMAP的总体几何均值改善，并用四个典型电路展示逐电路差异；不呈现置信区间、Wilcoxon检验、Holm校正、中位比或胜/平/负。相关推断统计仍保留在冻结证据文件中。
- 视界配对只解释最大视界改变后产生的整体边界决策差异；遗传搜索与逐坐标确定性贪心及同预算局部精修的配对只解释固定目标和物理可行域中的搜索差异。
- GA-LK 的有效结果须通过占位、AOD 次序和非目标交点检验；ZAC 与 ICCAD/QMAP 按各自实现的输出语义计分，不附加本文的物理可行性后处理。主表列重排时延，端到端编译时间另在实验节末段按固定12电路的计时口径报告。

## 自动与视觉验收

以下为当前稿必须重复执行的检查项；最新通过状态与文件身份以 `final_paper_qa.json` 和本轮升级记录为准，上一轮通过不能替代修改后的验收。

1. 执行 `python3 verify_paper_zh.py --compile --expected-pages 9 --evidence-root /Users/zhouzixiang/Desktop/zac/ZAC_zzx/results/paper_zh_v2 --json-output writing/final_paper_qa.json`。
2. 检查XeLaTeX/BibTeX构建无overfull、过大浮动体、未定义引用或文献，23个引用键解析，参考文献位于第九页。
3. 核对manifest中的Fig. 6派生文件、178个结果宏与独立单元；另执行 `writing/audit_numerical_presentation.py` 检查高精度百分比、基准规模、受控参数和时延排序。Table III和Fig. 6不手工抄录数值。
4. 检查 `figures/overall_framework.pdf` 为单页、无光栅对象、字体嵌入且无中文图内文字；高分辨率查看原子标号、区域边界与标签净空。
5. 渲染六张核心图与九页正文，检查裁切、越界、文字重叠、页边界和两栏对齐；核对图注在下、表题在上及首次引用距离。
6. 论文与总体流程图PDF的校验值统一查看 `final_paper_qa.json`，本文不重复写入易过时的固定哈希。
7. 作者已确认无经费资助；最终提交前仍需核对会议元数据、地址格式与邮箱。若发生修改，应重新执行同一编译与逐页视觉验收。

## 未解决证据风险

1. **QMAP154文件数与独立单元：**数据集包含154个文件和151份唯一规范内容；主比较已将共同有效的122个文件按规范电路SHA-256合并为120个独立簇，并以簇均值推断。完整结果率仍按154个文件报告，后续修改不得把文件数当作独立推断样本数。
2. **主比较共同样本：**主结果的连续指标仅纳入ZAC、ICCAD/QMAP和GA-LK三种主比较方法同时产生有效结果的电路。共同样本的规模和组成因而受三者共同有效条件影响，不能等同于数据集全量；需要持续披露各方法的失败数、排除原因及共同样本覆盖率。
3. **默认GA-LK配置的选择来源：**冻结配置足以重放现有结果，但默认参数如何从候选网格中确定、使用哪一开发划分和选择指标仍未形成完整可审计记录。该缺口影响超参数选择与留出评估的独立性判断。
4. **外部可访问性：**上一轮交付记录显示工程已同步GitHub、仓库为私有，Overleaf远端未启用Git access。本轮为独立副本的论文升级，未变更平台配置；正式分享前应按实时状态核对可访问性。
