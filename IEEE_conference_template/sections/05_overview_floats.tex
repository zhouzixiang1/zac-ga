% Declare wide result floats on the preceding page so IEEE two-column
% placement keeps them next to the opening experimental discussion.
\input{sections/05_main_table}
\begin{figure*}[!t]
  \centering
  \resizebox{\textwidth}{!}{\input{figures/experimental_summary}}
  \caption{GA-LK的编译质量与受控比较。(a) 相对两种基线的保真度几何均值增幅，ZAC18和QMAP154分别含\PaperRevision{\DefaultZACStrictN}和\PaperRevision{\DefaultQMAPStrictN}个共同有效电路。(b) 固定目标的遗传搜索与贪心比较，以及$H=8$与$H=0$的前瞻配置比较，分别含\AblationGAN 和\AblationHN 个有效电路。(c) \texttt{QFT-18}相对ZAC的对数保真度分量变化，正值表示对应损失减少。}
  \label{fig:experimental-summary}
\end{figure*}
